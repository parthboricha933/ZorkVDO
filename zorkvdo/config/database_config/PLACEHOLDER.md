# Database Configuration Placeholder
#
# Selects the persistence backend.
#
# Environment variables (see .env):
#   DATABASE_BACKEND                (memory | firestore)
#   FIRESTORE_CONNECTION_STRING     (optional — for direct connect)
#   FIREBASE_PROJECT_ID             (required for firestore backend)
#   FIREBASE_CREDENTIALS_PATH       (required for firestore backend)
#
# Status: PLACEHOLDER — defaults to in-memory store (dev/test only).
