# Firebase Configuration Placeholder
#
# This folder holds Firebase-related configuration.
#
# Files expected here in production:
#   - firebase-service-account.json   (backend; downloaded from Firebase Console →
#                                       Project Settings → Service Accounts →
#                                       Generate new private key)
#   - google-services.json            (Flutter Android app; downloaded from
#                                       Firebase Console → Project Settings →
#                                       Your apps → Android app → google-services.json)
#
# NEVER commit either file to git. Both are gitignored.
#
# The backend reads connection details from environment variables (see .env):
#   FIREBASE_API_KEY
#   FIREBASE_PROJECT_ID
#   FIREBASE_APP_ID
#   FIREBASE_STORAGE_BUCKET
#   FIREBASE_MESSAGING_SENDER_ID
#   FIREBASE_CREDENTIALS_PATH         (path to firebase-service-account.json)
#
# Status: PLACEHOLDER — drop real credentials here when ready.
