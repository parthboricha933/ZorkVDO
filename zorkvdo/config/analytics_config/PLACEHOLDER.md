# Analytics Configuration Placeholder
#
# Product analytics — what templates are popular, where users drop off, etc.
#
# Environment variables (see .env):
#   ANALYTICS_KEY                  (generic — used by whichever provider is enabled)
#   ANALYTICS_PROVIDER             (posthog | mixpanel | amplitude | none)
#   POSTHOG_API_KEY                (PostHog-specific)
#   POSTHOG_HOST                   (default: https://app.posthog.com)
#   SENTRY_DSN                     (error tracking — strongly recommended)
#
# Status: PLACEHOLDER — analytics disabled when key is missing.
