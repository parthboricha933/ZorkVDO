# Notification Configuration Placeholder
#
# Channels supported:
#   - In-app    (always on — uses the `notifications` collection)
#   - Email     (optional — set SMTP_* or RESEND_API_KEY)
#   - Push      (optional — set FCM_SERVER_KEY for mobile push)
#
# Environment variables (see .env):
#   FCM_SERVER_KEY                 (Firebase Cloud Messaging server key)
#   RESEND_API_KEY                 (optional — Resend.com for transactional email)
#   SMTP_HOST / SMTP_PORT / SMTP_USER / SMTP_PASSWORD  (alternative email backend)
#   NOTIFICATION_FROM_EMAIL        (From: address for email notifications)
#
# Status: PLACEHOLDER — in-app notifications work without any key.
