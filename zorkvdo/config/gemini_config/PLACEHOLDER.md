# Gemini / Google AI Configuration Placeholder
#
# This folder holds Gemini-specific configuration.
#
# Files expected here:
#   - model_config.json   (optional — model routing, generation params, safety settings)
#
# Environment variables (see .env):
#   GEMINI_API_KEY        (required for AI_PROVIDER=gemini)
#   GEMINI_MODEL          (default: gemini-1.5-pro)
#   GEMINI_VISION_MODEL   (default: gemini-1.5-pro)
#   GEMINI_BASE_URL       (optional — override endpoint)
#
# Status: PLACEHOLDER — set GEMINI_API_KEY in .env to enable.
